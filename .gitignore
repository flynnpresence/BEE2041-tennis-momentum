*.pyc
__pycache__/
.DS_Store
venv/
.venv/

# Data files: ignore contents but keep folder structure
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

# Auto-generated by Quarto
blog_files/

# Makefile build stamps
.download.stamp
.clean.stamp

# Agent tooling / session-local, never intended for the public repo
.claude/

# Inference diagnostics: evidence trail from the SE bootstrap investigation
# (outlier rates, LinearDML-vs-forest comparisons, per-spec replicate CSVs).
# Kept locally for writing the methodology doc; not official pipeline output
# and expensive to regenerate (~67 min), so gitignored rather than deleted.
diagnostics/
